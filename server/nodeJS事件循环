事件循环，观察者，请求对象，I/O线程池这四者共同构成了Node异步I/O的关键字：单线程，
事件循环，观察者，I/O线程池，由于我们知道Javascript是单线程，所以按常识很容易理解
为它不能充分利用多核CPU，事实上，在Node中，除了JavaScript是单线程外，Node自身其实是多线程的
只是I/O线程使用CPU较少。另一个需要重视的观点则是，除了用户代码无法并行执行外，所有的I/O(磁盘I/O和网络I/O)